int main()
{
  int x=unknown(); int y=unknown(); int z =1;
  if(x<=0) return 0;
  if(y>=0) return 0;
  while(y<x) {
    int tx = x + 1;
    int ty=y+2;
    int tz=z;
    if(y>=0) tz=z*2;
    else tw=w-2;
    x=tx;
    y=ty;
    z=tz;
  }
  assert(z>1);
}
